# Render Environment Variables for External Databases
# Copy this file to .env and fill in your cloud database credentials

# MongoDB Atlas (Free Tier - 512MB)
# Get from: https://cloud.mongodb.com (create free cluster)
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/orders_db?retryWrites=true&w=majority

# MySQL Database (Use one of these free options):
# Option 1: PlanetScale (MySQL-compatible, requires card for verification)
MYSQL_HOST=aws.connect.psdb.cloud
MYSQL_PORT=3306
MYSQL_DATABASE=your_database_name
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password

# Option 2: Railway MySQL (if you have Railway account)
# MYSQL_HOST=containers-us-west-1.railway.app
# MYSQL_PORT=XXXX
# MYSQL_DATABASE=railway
# MYSQL_USER=root
# MYSQL_PASSWORD=your_railway_mysql_password

# Application Settings
NODE_ENV=production

# Note: Kafka and Debezium services are removed for Render deployment
# to fit within free tier limits (512MB RAM)
# Use direct database connections instead of CDC pipeline
